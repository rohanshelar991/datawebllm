# For Streamlit Community Cloud or local secrets:
# - Do NOT commit real keys. Keep `.streamlit/secrets.toml` ignored by git.
# - On Streamlit Cloud: set this in the app "Secrets" UI instead.
GROQ_API_KEY="replace_me"
